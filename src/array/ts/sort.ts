// sort() 方法用原地算法对数组的元素进行排序，并返回数组。排序算法现在是稳定的。默认排序顺序是根据字符串Unicode码点。

const sort = (arr: any[], fn) => {
  for (let i = 0;i < arr.length;i++) {
    for (let j = i;j < arr.length - i;j++) {

    }
  }
}

export default sort